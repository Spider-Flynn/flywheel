# flywheel 维护规则

## 定位

个人能力的开源出口。内容从私有孵化仓迁入，迁入前已完成通用化与隐私检查。

## 结构

- 能力单元放在 `skills/<name>/`，一个目录就是一份可以独立拿走的东西。
- 单元自包含：`SKILL.md` 是给 AI 的执行契约，`README.md` 说明用途与核心机制，其余资源都放在
  单元目录内，不引用单元外的路径。
- 目录名用小写字母、数字和连字符。
- `README.md` 的索引是一张表，列为"能力"和"解决什么问题"，链接指向单元目录。迁入或删除单元
  时，同一次提交里更新索引。

## 维护

- 对外内容以通用性优先，不为单人使用习惯引入特殊约定。
- 内容变更遵循用户级通用规则；未明确要求 `commit` 或 `push` 时，修改只留在工作区。
- 发现已发布内容存在隐私泄露或授权问题时，优先修复或移除。
