# flywheel 维护规则

## 定位

个人能力的开源出口。内容从私有孵化仓迁入，迁入前已通过通用化与隐私门禁；门禁标准由孵化仓的
`hatch` skill 定义，本仓库不重复规定。

## 文档

- `README.md` 面向使用者，只说明这个仓库提供哪些能力、每个能力解决什么问题；不放使用指导、
  来源说明和授权声明。
- `README.md` 的索引是一张表，列为"能力"和"解决什么问题"，链接指向单元或组件目录。迁入或
  删除内容时，同一次提交里更新索引。
- 维护本仓库时同时读取 `README.md`，内容变化要反映到索引，两份文件保持一致。

## 结构

- 能力有两种形态：单个能力单元放在 `skills/<name>/`；成套组件放在根目录 `<组件名>/`，由多个
  互相协作的单元组成，内部可以互相引用，对外仍是一个整体。
- 一个目录就是一份可以独立拿走的东西。所有资源都放在自己目录内，不引用自己目录外的路径。
- 独立单元配 `SKILL.md` 和 `README.md`：前者是给 AI 的执行契约，后者说明用途与核心机制。
  成套组件只在组件根目录放一份 `README.md`，说明这一套是什么、由哪些单元组成、怎么组合使用，
  内部单元只需要 `SKILL.md`。
- 目录名用小写字母、数字和连字符。

## 维护

- 内容变更遵循用户级通用规则；未明确要求 `commit` 或 `push` 时，修改只留在工作区。
- 发现已发布内容存在隐私泄露或授权问题时，优先修复或移除。
